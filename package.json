{
  "name": "barrier-management-bowtie",
  "private": true,
  "version": "0.0.0",
  "description": "Barrier Management — Bowtie · ISO 31000-aligned bowtie & barrier management SaaS module.",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20",
    "pnpm": "9.x"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "test:cov": "turbo run test:cov",
    "test:e2e": "turbo run test:e2e --filter=web",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "db:migrate": "pnpm --filter web exec prisma migrate deploy",
    "db:migrate:dev": "pnpm --filter web exec prisma migrate dev",
    "db:seed": "pnpm --filter web exec tsx prisma/seed.ts",
    "db:reset": "pnpm --filter web exec prisma migrate reset --force --skip-seed && pnpm db:seed",
    "db:studio": "pnpm --filter web exec prisma studio",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "prettier": "^3.3.3",
    "turbo": "^2.1.3",
    "typescript": "^5.6.3"
  }
}
