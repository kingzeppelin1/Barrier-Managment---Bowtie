# @bowtie/config

Shared configuration fragments — eslint preset, prettier config, tsconfig fragments — re-used by every app and package. No runtime imports.

See [`docs/08_PACKAGE_STRUCTURE.md`](../../docs/08_PACKAGE_STRUCTURE.md) §4.
