# @bowtie/shared

Canonical zod schemas, TypeScript types, enums, and constants used across `apps/web`, `apps/worker`, and the other packages. Define a domain shape **once here**; everything else imports it.

See [`docs/08_PACKAGE_STRUCTURE.md`](../../docs/08_PACKAGE_STRUCTURE.md) §4–§5.
