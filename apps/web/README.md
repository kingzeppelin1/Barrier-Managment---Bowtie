# apps/web

Next.js 15 (App Router) — UI + API for Barrier Management — Bowtie. Deployed to Vercel.

The current foundation renders a "Hello" page at `/`. Domain pages, the 12-step Wizard, and the Bowtie canvas land in later sprints — see [`docs/prompts/`](../../docs/prompts/).

See [`docs/08_PACKAGE_STRUCTURE.md`](../../docs/08_PACKAGE_STRUCTURE.md) §2 for the planned `app/`, `lib/`, and `prisma/` layout.
